---
phase: 08-queue-mode
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - config/config.example.yaml
  - docs/API.md
autonomous: true

must_haves:
  truths:
    - "config.example.yaml documents queueMode, queueMaxWaitMs, queueMaxSize settings"
    - "API docs describe queue behavior and new response fields"
  artifacts:
    - path: "config/config.example.yaml"
      provides: "Queue mode settings with comments explaining behavior"
      contains: "queueMode"
    - path: "docs/API.md"
      provides: "Queue mode documentation in API reference"
      contains: "queue"
  key_links: []
---

<objective>
Update config example and API docs to document queue mode settings and behavior.

Purpose: Users need to know how to enable and configure queue mode, and what to expect from the API when it is active.
Output: Updated config.example.yaml and API docs.
</objective>

<execution_context>
@C:/Users/danen/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-queue-mode/08-RESEARCH.md
@config/config.example.yaml
@docs/API.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config example and API documentation</name>
  <files>config/config.example.yaml, docs/API.md</files>
  <action>
1. In `config/config.example.yaml`, add queue mode settings to the settings block with comments:
   ```yaml
   # Queue mode: hold requests when all providers exhausted instead of returning 503
   # When enabled, requests wait for a provider to come off cooldown
   # queueMode: false           # Enable/disable queue mode (default: false)
   # queueMaxWaitMs: 300000     # Max time a request waits in queue (default: 5 minutes)
   # queueMaxSize: 100          # Max queued requests per chain (default: 100)
   ```
   Keep them commented out (showing defaults) to match the pattern of other optional settings in the file.

2. In `docs/API.md`, add a "Queue Mode" section:
   - Explain what queue mode does (requests wait instead of 503)
   - Configuration settings and their defaults
   - Behavior: FIFO order, single-item drain on cooldown expiry, timeout returns 503 with `queue_timeout` code, full queue returns 503 with `queue_full` code
   - Note: streaming and non-streaming both queue the same way
   - Note: queue is in-memory, does not survive restarts (graceful shutdown rejects all queued items)
   - Updated /v1/ratelimits response example showing `queue` field with stats
   - Error response examples for queue_timeout and queue_full
  </action>
  <verify>Verify config file is valid YAML: `node -e "const yaml = require('yaml'); const fs = require('fs'); yaml.parse(fs.readFileSync('config/config.example.yaml', 'utf8'));"` (or equivalent). Verify docs file exists and contains queue section.</verify>
  <done>Config example shows queue settings with explanatory comments. API docs have queue mode section with configuration, behavior description, and error examples.</done>
</task>

</tasks>

<verification>
- Config example is valid YAML
- API docs contain queue mode documentation
</verification>

<success_criteria>
- Users can discover queue mode settings from config.example.yaml
- API docs explain queue behavior, configuration, error codes, and ratelimits response format
</success_criteria>

<output>
After completion, create `.planning/phases/08-queue-mode/08-03-SUMMARY.md`
</output>
